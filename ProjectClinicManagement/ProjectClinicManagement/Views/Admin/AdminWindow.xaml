<Window x:Class="ProjectClinicManagement.Views.Admin.UserManagement"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectClinicManagement.ViewModel"
        mc:Ignorable="d"
        Title="UserManagement" Height="450" Width="800">
    <Window.DataContext>
        <local:UserViewModel />
    </Window.DataContext>
    <Grid>
        <ListBox ItemsSource="{Binding Accounts}" SelectedItem="{Binding Account}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding UserName}" Width="200"/>
                        <TextBlock Text="{Binding Email}" Width="200"/>
                        <TextBlock Text="{Binding Salary}" Width="200"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
            <Button Content="Add User" Command="{Binding AddUserCommand}" Width="100" Margin="5"/>
            <Button Content="Update User" Command="{Binding UpdateUserCommand}" Width="100" Margin="5"/>
            
        </StackPanel>
    </Grid>
</Window>
